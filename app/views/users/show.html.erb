<html>
	<body>
		<h3> Perfil de <%= @user.pessoa.nome %> </h3>

		<% if current_user == @user %>
			<ul>
				<li><%= link_to "Editar", edit_user_path(current_user) %></li>
				<li><%= link_to "Apagar", @user, method: :delete%></li>
			</ul>
		<%end%>
	</body>
</html>